var isMatch = function(s, p) {
    // compare "S" and "P"
    // S = english
    // P = could contain english . *
    // if P does not contian . , *
    // S != P return false
    // s == p length is not match return false;

    if(s.length !== p.length ) return false;

};